metadata:
  name: Error Demo
  author: TIA

parameters:
  width: 100

parts:
  plate:
    primitive: box
    parameters:

      width: 10

      height: 20

      depth: 30

  hole:
    primitive: cylinder
    radius: 5
    height: 35  # Taller than plate for clean cut

operations:
  bad_operation:
    type: boolean
    operation: difference
    base: plate
    subtract: [nonexistent_part]  # This will cause an error!
